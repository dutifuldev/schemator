# OpenClaw Model Profile RFC Review Example

These files are example Schemator-style review artifacts created while
reviewing the data models proposed in OpenClaw RFC 0009, Model Harness Profiles.

They show the current Markdown report shape that should eventually be generated
from structured JSON review artifacts.

- [Example project context prompt](project-context-prompt.md)
